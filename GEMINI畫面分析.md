# GEMINI 畫面分析.md

---

## 1. 頁面目標

本頁面為「座位總覽與預訂管理」頁面，提供使用者以視覺化的方式查看座位狀態、管理現有預訂，並快速新增訂位。

---

## 2. 結構佈局（Layout）

採用三欄式佈局，從左至右分別為：

1.  **訂位列表區（左欄）**：顯示當日的訂位清單。
2.  **座位圖區（中欄）**：以格狀呈現所有座位，並標示出已被預訂的時段。
3.  **功能操作區（右欄）**：提供編輯模式、新增桌子等管理功能。

| 區塊 | 元件/模組 | 備註 |
| --- | --- | --- |
| **導覽列** | `layouts/default/navbar.vue` | 全域共用，包含「返回儀表板」、「返回點餐」、「回到首頁」等按鈕。 |
| **左欄** | `components/BookingList.vue` | 訂位列表，包含搜尋、新增按鈕及預訂資訊卡片。 |
| **中欄** | `components/SeatMapGrid.vue` | 座位總覽格狀圖，每個座位卡片顯示桌號與預訂狀態。 |
| **右欄** | `components/ControlPanel.vue` | 包含「編輯模式」、「新增桌子」、「整理對齊」等管理按鈕。 |

---

## 3. 元件拆解（Component Breakdown）

### 1) BookingList.vue

-   **目標**：顯示與管理訂位清單。
-   **子元件**：
    -   `v-text-field`：用於搜尋。
    -   `v-btn`：「新增訂位」。
    -   `v-list`：顯示訂位項目。
    -   `BookingCard.vue`（建議）：單筆訂位資訊卡。
-   **資料**：
    -   `bookings`: 訂位陣列 `[{ name, phone, table, time, pax }]`
-   **互動**：
    -   點擊「新增訂位」彈出對話框。
    -   搜尋框輸入可過濾列表。

### 2) SeatMapGrid.vue

-   **目標**：視覺化呈現座位分佈與狀態。
-   **子元件**：
    -   `TableCard.vue`（建議）：代表單張桌子，顯示桌號與預訂資訊。
-   **資料**：
    -   `tables`: 桌位陣列 `[{ id, name, bookings: [{ time, pax }] }]`
-   **互動**：
    -   點擊桌子可查看詳細資訊或進行操作。

### 3) ControlPanel.vue

-   **目標**：提供管理座位圖的快捷操作。
-   **子元件**：
    -   `v-btn`：「新增桌子」、「整理對齊」。
    -   `v-switch`：「編輯模式」。
-   **互動**：
    -   開啟「編輯模式」後，可拖曳調整座位位置。
    -   點擊「新增桌子」會在座位圖區增加一張新桌子。

---

## 4. 資料模型（Data Model）

```json
{
  "tables": [
    {
      "id": "A1",
      "name": "A1",
      "bookings": []
    },
    {
      "id": "A3",
      "name": "A3",
      "bookings": [
        { "time": "12:00", "pax": 2 },
        { "time": "15:00", "pax": 4 }
      ]
    }
  ],
  "bookings": [
    {
      "name": "王曉明",
      "phone": "0979913380",
      "tableId": "A3",
      "time": "12:00",
      "pax": 2
    },
    {
      "name": "林小美",
      "phone": "0912345678",
      "tableId": "A3",
      "time": "15:00",
      "pax": 4
    }
  ]
}
```

---

## 5. i18n Key 規劃

| Key | 中文 |
| --- | --- |
| `page.seatMap.title` | 定位總覽 |
| `btn.addBooking` | 新增訂位 |
| `btn.addTable` | 新增桌子 |
| `label.search` | 搜尋姓名/電話/桌號 |
| `label.tableName` | 桌號 |
| `label.bookingTime` | 時間 |
| `label.pax` | 人數 |

---

## 6. 驗收清單

- [ ] 點擊導覽列「定位總覽」能跳轉至此頁面。
- [ ] 頁面正確顯示左、中、右三個區塊。
- [ ] 座位圖能根據資料顯示正確的預訂狀態。
- [ ] 訂位列表能正確顯示當天的所有訂位。
